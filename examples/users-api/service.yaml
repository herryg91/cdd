version: 1
lang: go
name: users-api
contract: 
  proto-files:
    - grpc/proto/users.proto
dependencies:
  services:
    - ../location-api/service.yaml

# default examples 
# contract: 
#   config:
#     output-grst: grpc/pb/
#     output-scaffold-mysql:  
#     output-dependency: grpc/pb-deps/
#   proto-files:
#     - grpc/proto/province.proto

# export GO111MODULE=on && export GOPROXY=direct && export GOSUMDB=off && go mod tidy